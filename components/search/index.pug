mixin search(query)
    form(action='/home/search').field.has-addons
        .control
            input.input(type='text' name='query' value=query placeholder='search for your ducks')
        .control
            a.button.is-info
                | Search
